import {Options} from "../../Options";

export const serverPath = Options.serverPath + 'a.php';
export const chatPath = Options.serverPath + 'achat.php';
export const combatPath = Options.serverPath + 'c.php';
export const indexPath = Options.serverPath + '/index.php';
export const htmlPath = Options.serverPath + 'html.php';
